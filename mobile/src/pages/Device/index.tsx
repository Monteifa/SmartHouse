import React from 'react';
import { Text, View } from 'react-native';

import Header from '../../components/Header';

import styles from './styles';

const Device = () => {
  return <View style={styles.container}>{/* <Header name={name} /> */}</View>;
};

export default Device;
